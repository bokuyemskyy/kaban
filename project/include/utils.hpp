#ifndef UTILS_HPP
#define UTILS_HPP

#include <cmath>
#include <utility>

class Utils {
   public:
    static float distance(const std::pair<float, float> &a, const std::pair<float, float> &b);
};

#endif